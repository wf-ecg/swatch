(function ($) {

    $.fn.track = function (opt) {
       // upon events take coordinates
       // store using ...
       // reposition on open
       // provide clear button
    }

    $('p:not(.static)').track();

})(jQuery);
